spring:
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        id:
          new_generator_mappings: true
  flyway:
    enabled: true

jwt:
  secret: test-secret-key-must-be-at-least-256-bits-long-for-hs256-algorithm-xyz
  expiration-hours: 1
